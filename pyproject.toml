[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "pixelworld"
version = "0.6.1"
description = "Reusable PixelWorld 0.6.1 generation, training, evaluation, and inference core"
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.26,<3",
  "torch>=2.2,<3",
]

[project.scripts]
pixelworld = "pixelworld.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
markers = [
  "golden: full Seed-42 reference integration test",
]
